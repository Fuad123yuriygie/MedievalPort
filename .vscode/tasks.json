{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build", // This label is referenced in launch.json
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}